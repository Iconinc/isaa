/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
        "i.pinimg.com",
        "cdn.dribbble.com/userupload",
        "cdn.dribbble.com"
    ],
  },
};

module.exports = nextConfig;
